# Build a full TF-M (all secure services) instead of the minimal nRF91 default
# by selecting the base TF-M profile. BL2 itself is controlled per test case
# (CONFIG_TFM_BL2), see tests.yaml.
CONFIG_TFM_PROFILE_TYPE_NOT_SET=y
